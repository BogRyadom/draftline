# Runtime dependencies (installed on Render).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT>=2.9,<3.0
cryptography>=43,<47
httpx>=0.27,<1.0

# Database (Phase 1+): async SQLAlchemy over asyncpg.
SQLAlchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29,<0.31

# Gmail OAuth + API (Phase 1).
google-auth-oauthlib>=1.2,<2.0
google-api-python-client>=2.130,<3.0

# LLM (Phase 2+): Groq via the OpenAI-compatible client.
openai>=1.40,<3.0
