# Normalize line endings: LF in the repo, checked out as-is per platform.
# Keeps Windows dev and Linux (Render/Vercel) builds consistent.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
